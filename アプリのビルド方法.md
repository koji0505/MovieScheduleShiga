# アプリのビルド方法

## 共通の前提

```bash
cd app
npx expo login   # 未ログインの場合
```

---

## iPhone (iOS)

### ビルドコマンド

```bash
eas build --platform ios --profile preview
```

### 特徴・注意点

| 項目 | 内容 |
|---|---|
| 出力形式 | `.ipa` ファイル |
| ビルド場所 | EAS クラウド（Apple のサーバー使用） |
| 必要なもの | **Apple Developer アカウント**（年額$99）|
| 署名 | EAS が自動で証明書・プロビジョニングプロファイルを管理 |
| インストール方法 | EAS のダウンロードリンク → QR コードでインストール（Ad Hoc 配布） |
| 制限 | インストールできる端末は事前登録が必要（最大100台） |

### インストール時の注意

iPhoneで初回インストール後、**設定 → 一般 → VPNとデバイス管理** で開発者を信頼する操作が必要。

---

## Android

### ビルドコマンド

```bash
eas build --platform android --profile preview
```

### 特徴・注意点

| 項目 | 内容 |
|---|---|
| 出力形式 | `.apk` ファイル（`preview` プロファイル） |
| ビルド場所 | EAS クラウド |
| 必要なもの | EAS アカウントのみ（無料で可） |
| 署名 | EAS が自動でキーストアを管理 |
| インストール方法 | EAS のダウンロードリンク → APK を直接転送 → インストール |

### インストール時の注意

Android 端末で「提供元不明のアプリ」のインストールを許可する設定が必要（設定 → セキュリティ）。

---

## プロファイル一覧（参考）

| プロファイル | iOS | Android | 用途 |
|---|---|---|---|
| `development` | - | - | 開発用クライアント |
| `preview` | `.ipa`（実機配布） | `.apk`（サイドロード） | **個人インストール用** |
| `production` | App Store 用 | `.aab`（Play Store 用） | ストア公開用 |

---

## 両方同時にビルドする場合

```bash
eas build --platform all --profile preview
```

ビルド完了後、EAS ダッシュボード（https://expo.dev）またはCLIに表示されるダウンロードリンクからファイルを取得できます。
