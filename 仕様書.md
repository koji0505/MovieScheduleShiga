# 滋賀映画スケジュール検索 仕様書

作成日: 2026-02-24
最終更新: 2026-03-18

---

## 1. プロジェクト概要

### 目的
滋賀県内5館の映画館の上映スケジュールを一覧で検索・確認できるスマートフォンアプリ。

### 対象映画館
| 映画館名 | MOVIE WALKER ID |
|---------|----------------|
| ユナイテッド・シネマ大津 | th524 |
| イオンシネマ草津 | th249 |
| イオンシネマ近江八幡 | th393 |
| 水口アレックスシネマ | th525 |
| 彦根ビバシティシネマ | th138 |

### 配布形態
- **iOS**: TestFlight（現在）→ App Store（MOVIE WALKER 許諾取得後）
- **Android**: Google Play（予定）
- **Web**: PWA（https://koji0505.github.io/MovieScheduleShiga/）

---

## 2. システム構成

```
[press.moviewalker.jp]            [各館公式サイト]
        ↓ スクレイピング                 ↓ 直接スクレイピング（補完用）
        └──────────── マージ ────────────┘
                    ↓（毎日JST 5:00）
             [GitHub Actions]
                    ↓ JSON生成・ポスター画像ダウンロード・コミット
             [GitHub Pages]
               docs/data/schedules.json
               docs/data/posters/{movie_id}.jpg
               docs/index.html（app.jsバージョン更新）
                    ↓ fetch（キャッシュバスティング付き）
             [スマホアプリ / PWA]
```

サーバーレス構成。バックエンドサーバー・DBは不要。

---

## 3. データ収集（スクレイピング）

### ファイル
- `scraper/scrape.py` — スクレイパー本体
- `scraper/requirements.txt` — Python依存パッケージ（requests, beautifulsoup4, playwright）
- `.github/workflows/scrape.yml` — GitHub Actions ワークフロー

### 動作
- **実行タイミング**: 毎日 JST 5:00（UTC 20:00）、手動実行も可能
- **第1段階**: MOVIE WALKER から全5館のスケジュールを取得（SSR HTML をパース）
- **第2段階**: 各館の公式サイトから直接スクレイピングし、MW 未掲載分をマージ（2026-03-18 追加）

### 3.1 MOVIE WALKER スクレイピング

- **対象URL**: `https://press.moviewalker.jp/{theater_id}/schedule/`
- **パース方法**: BeautifulSoup4 で HTML を直接解析
- **取得データ**:
  - 映画タイトル
  - MOVIE WALKER 映画ID（`/mv{id}/` 形式のリンクから抽出）
  - ポスター画像URL（`<img>` タグの `src` 属性から抽出）
  - 上映日（`YYYY-MM-DD` 形式に変換）
  - 上映時刻リスト
- **課題**: MW の更新が遅れる場合があり、直近数日〜1週間程度のデータしか取得できないことがある

### 3.2 直接スクレイピング（補完用）（2026-03-18 追加）

MW で更新が遅い館について、各館の公式サイトから直接スケジュールを取得し、MW データにない映画・日付のみを補完する。

| 映画館 | 取得方法 | データソース |
|--------|---------|------------|
| イオンシネマ近江八幡 | JSON API | `theater.aeoncinema.com/schedule/v2/data/oumihachiman/schedule.json` |
| イオンシネマ草津 | JSON API | `theater.aeoncinema.com/schedule/v2/data/kusatsu/schedule.json` |
| ユナイテッド・シネマ大津 | Playwright + HTML | `unitedcinemas.jp/otsu/daily.php?date={date}` （1日ずつ取得、最大10日分） |
| 水口アレックスシネマ | JSON API | `schedule.alex-cinemas.com/schedule/data/schedule.json` |
| 彦根ビバシティシネマ | requests + HTML | `vivacitycinema.co.jp/schedule/` （週間スケジュール、日付範囲を展開） |

**取得範囲**: 今日から10日分（ビバシティはページに掲載されている全期間）

**マージ仕様**:
- タイトルを NFKC 正規化（半角カナ→全角カナ等）して MW データと照合
- MW にない映画 → そのまま追加
- MW にある映画 → MW にない日付のスケジュールのみ追加

**上映時刻の変換**:
- イオンシネマ: UTC の `startDate` を JST（+9時間）に変換
- ユナイテッドシネマ: JS レンダリングのため Playwright（Headless Chromium）を使用。`#dailyList li` の出現を待機後にパース。データがない日でストップ
- アレックスシネマ: 時刻キー（`HHMM` 形式）を `HH:MM` に変換

### 3.3 ポスター画像の処理

スクレイプ実行のたびに以下の処理を行う:
1. `docs/data/posters/` ディレクトリを削除して再作成
2. 各映画のポスター画像を `{movie_id}.jpg` としてダウンロード保存
3. `poster_url` を GitHub Pages の URL に書き換えて JSON に保存

**変更理由（2026-03-04）**: `press.moviewalker.jp` の画像を Android ネイティブアプリから直接参照すると表示されない問題が発生したため、自前配信に切り替えた。

**バリアント間ポスター共有（2026-03-18 追加）**:
「映画X（吹替）」と「映画X（字幕）」のように同一作品の別バージョンは、`base_title()` で正規化した基本タイトルが一致すれば同じポスター画像を共有する。ダウンロードは1回のみ。

`base_title()` の正規化内容（繰り返し適用）:
- 先頭の `[吹替]` `[字幕]` 等を除去
- 末尾の `（吹替）` `（字幕）` 等を除去
- 末尾の `4DX` `4DX2D` `IMAX` `MX4D` `4K` `3D` `ScreenX` `Dolby` 等を除去
- 全角・半角スペースをすべて除去して比較

**容量への影響**: 同一内容の画像は git が blob を重複排除するため、現在上映中の映画が変わらない限り容量は増加しない。年間ユニーク映画 ~100本 × ~80KB = 約 8MB/年 の増加見込み。

### 3.4 PWA キャッシュ対策（2026-03-18 追加）

- `schedules.json` の fetch に `?v=Date.now()` を付与（ブラウザキャッシュをバイパス）
- スクレイパー実行のたびに `docs/index.html` 内の `<script src="app.js?v={timestamp}">` を更新し、スマホ PWA が古い `app.js` をキャッシュし続ける問題を解消
- GitHub Actions の `git add` に `docs/index.html` を追加

### 出力 JSON フォーマット
```json
{
  "updated_at": "2026-03-04T05:00:00",
  "theaters": {
    "ユナイテッド・シネマ大津": [
      {
        "title": "映画タイトル",
        "movie_id": "123456",
        "poster_url": "https://koji0505.github.io/MovieScheduleShiga/data/posters/123456.jpg",
        "schedule": [
          { "date": "2026-03-04", "times": ["10:00", "13:00", "16:00"] }
        ]
      }
    ]
  }
}
```

### 保存先
- `docs/data/schedules.json`（GitHub Pages 経由で公開）
- `docs/data/posters/{movie_id}.jpg`（GitHub Pages 経由で公開）
- URL: `https://koji0505.github.io/MovieScheduleShiga/data/schedules.json`

---

## 4. スマホアプリ（React Native）

### 技術スタック
| 項目 | 内容 |
|------|------|
| フレームワーク | React Native（Expo SDK 54） |
| React バージョン | React 19 |
| React Native バージョン | 0.81.5 |
| ビルドツール | EAS Build |
| 言語 | JavaScript（JSX） |

### アプリ情報
| 項目 | 内容 |
|------|------|
| アプリ名 | 映画スケジュール検索 |
| 表示タイトル | 滋賀映画スケジュール検索 |
| iOS Bundle ID | com.koji0505.moviescheduleshiga |
| Android Package | com.koji0505.moviescheduleshiga |
| EAS Project ID | 11242bf8-8ad0-441b-a6f9-afc4d97e1ef1 |
| EAS Slug | movie-schedule-shiga |

### ファイル構成
```
app/
├── App.js                    # エントリーポイント（NavigationContainer）
├── app.json                  # Expo 設定
├── eas.json                  # EAS Build 設定
├── constants.js              # 定数（カラー、データURL）
├── context/
│   └── ScheduleContext.js    # データ共有コンテキスト（単一フェッチ）
├── hooks/
│   └── useSchedule.js        # データ取得・状態管理カスタムフック
├── screens/
│   ├── HomeScreen.js         # ホーム画面（ポスター2列グリッド）
│   └── SearchScreen.js       # 検索画面（タイトル・劇場・日付フィルター）
├── utils/
│   └── schedule.js           # データ変換・フィルタリングユーティリティ
├── components/
│   ├── MovieCard.js          # 映画カード表示コンポーネント（ポスター付き）
│   ├── DateFilter.js         # 日付フィルターコンポーネント
│   └── TheaterFilter.js      # 劇場フィルターコンポーネント
└── assets/
    ├── icon.png              # アプリアイコン（1024×1024、青背景カチンコ）
    ├── adaptive-icon.png     # Android アダプティブアイコン
    └── splash-icon.png       # スプラッシュ画面アイコン
```

### 主要定数（constants.js）
```javascript
DATA_URL = 'https://koji0505.github.io/MovieScheduleShiga/data/schedules.json'

COLORS = {
  primary:    '#1565C0',   // メインカラー（青）
  accent:     '#FF9800',   // アクセントカラー（オレンジ、ナビゲーションボタン）
  background: '#f0f2f5',
  white:      '#ffffff',
  border:     '#dddddd',
  cardBg:     '#f5f5f5',
  textLight:  '#666666',
  textMuted:  '#999999',
  error:      '#e74c3c',
}
```

---

## 5. 機能仕様

### 5.1 データ取得
- アプリ起動時に `schedules.json` を fetch（ScheduleContext で全画面共有、1回のみ）
- ローディング中はスピナー表示
- 取得失敗時はエラーメッセージ＋「再試行」ボタン表示
- プルダウンで手動更新（Pull to Refresh）
- 最終更新日時をヘッダーに表示（JST ロケール）

### 5.2 ホーム画面
- 上映中の映画一覧をポスター付き2列グリッドで表示
- タップで検索画面へ遷移（タイトルを初期値として渡す）
- 「詳細」リンクで MOVIE WALKER 映画詳細ページを開く（`https://moviewalker.jp/mv{id}/`）
- ポスター未取得時はプレースホルダー表示
- ヘッダー右端にオレンジの「🔍 検索」ボタン

### 5.3 検索画面
- テキスト入力でリアルタイム絞り込み（部分一致）
- 入力クリアボタン付き（iOS）
- ヘッダー右端にオレンジの「🏠 ホーム」ボタン

### 5.4 劇場フィルター
- 横スクロールのボタンリスト（5館 + 「すべての劇場」）
- 選択中のボタンは青くハイライト

### 5.5 日付フィルター
- 横スクロールのボタンリスト
- 「すべて」ボタンで全日程表示
- 日付ボタン（例: `2/25(水)`）で特定日に絞り込み
- 選択中のボタンは青くハイライト

### 5.6 映画カード表示（検索結果）
- ポスター画像をカード上部に表示（未取得時は青いバー）
- タイトル・映画館名・上映日・上映時刻を表示
- 複数映画館で上映中の映画は1カードにまとめて表示
- カードデザイン: 白背景、角丸、影付き

---

## 6. データ処理ロジック（utils/schedule.js）

| 関数 | 処理内容 |
|------|---------|
| `buildMovieIndex(data)` | theaters別データを映画タイトルをキーとした辞書に変換（`poster_url` 含む） |
| `buildMovieList(data)` | 重複なし映画リストを生成（ホーム画面用、タイトル昇順） |
| `getAvailableDates(data)` | 全映画館の全日付を重複なく昇順で返す |
| `filterMovies(movieIndex, query, selectedDate, selectedTheater)` | タイトル・日付・劇場でフィルタリングし表示用リストを返す |
| `formatDate(dateStr)` | `'YYYY-MM-DD'` → `'M/D(曜)'` に変換 |

---

## 7. EAS Build 設定

```json
{
  "build": {
    "development": { "developmentClient": true, "distribution": "internal" },
    "preview":     { "distribution": "internal", "android": { "buildType": "apk" } },
    "production":  { "autoIncrement": true }
  }
}
```

| プロファイル | iOS | Android | 用途 |
|---|---|---|---|
| `development` | - | - | 開発用クライアント |
| `preview` | `.ipa`（実機配布） | `.apk`（サイドロード） | 個人インストール用 |
| `production` | App Store 用 | `.aab`（Play Store 用） | ストア公開用 |

- **preview** ビルド: Android は APK 形式（`buildType: "apk"`）で出力（2026-03-04 追加）
- **production** ビルド: バージョン自動インクリメント
- iOS 暗号化: `ITSAppUsesNonExemptEncryption: false`（app.json に設定済み）

### ビルドコマンド
```bash
cd app
eas build --platform android --profile preview  # Android APK
eas build --platform ios --profile preview       # iOS IPA
eas build --platform all --profile preview       # 両方同時
```

---

## 8. デザイン

| 要素 | 内容 |
|------|------|
| メインカラー | `#1565C0`（青） |
| スプラッシュ背景 | `#1a1a2e`（ダークネイビー） |
| アイコン | カチンコ（青背景） |
| 画面向き | 縦固定（portrait） |
| ステータスバー | light-content（白文字） |

---

## 9. 今後の検討事項

### 短期
- [ ] MOVIE WALKER への利用許諾申請
- [ ] 許諾取得後 App Store / Google Play 一般公開

### 機能拡張候補
- [x] 映画ポスター画像の表示（2026-02-25 実装済み）
- [x] 「現在公開中」映画一覧画面（ホーム画面として実装済み）
- [x] React Navigation による複数画面対応（2026-02-25 実装済み）
- [ ] 各映画館の公式サイトへのリンク

### データソース
- [x] 各映画館の公式サイトから直接スクレイピングして MW 未掲載分を補完（2026-03-18 実装済み）
  - イオンシネマ2館・水口アレックスシネマ: JSON API
  - ユナイテッドシネマ大津: Playwright（JS レンダリング対応）
  - 彦根ビバシティシネマ: HTML 週間スケジュールページ

---

## 10. リポジトリ構成

```
MovieScheduleShiga/
├── .github/
│   └── workflows/
│       └── scrape.yml          # GitHub Actions ワークフロー
├── scraper/
│   ├── scrape.py               # スクレイパー本体
│   └── requirements.txt        # Python依存パッケージ
├── docs/                       # GitHub Pages 公開ディレクトリ
│   ├── index.html              # PWA
│   ├── app.js                  # PWA JavaScript
│   ├── style.css               # PWA スタイル
│   └── data/
│       ├── schedules.json      # スクレイピング結果データ
│       └── posters/            # ポスター画像（スクレイプ毎に全削除→再保存）
│           └── {movie_id}.jpg
└── app/                        # React Native アプリ
    └── （§4 参照）
```

---

## 11. 注意事項

- スクレイピング対象は `press.moviewalker.jp`（SSR、JSON API なし）
- MOVIE WALKER 利用規約 第6条: 個人利用に限定、再配布・公衆送信を禁止
- App Store 一般公開前に MOVIE WALKER への許諾申請が必要
- GitHub Actions の無料枠: パブリックリポジトリは無制限、プライベートは月2,000分
